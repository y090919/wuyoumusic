{
  "name": "wuyou-music-player",
  "version": "1.0.0",
  "description": "无忧音乐播放器 - 在线音乐网站",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "node build-exe.js && npx esbuild server.js --bundle --platform=node --outfile=bundle.js && node --experimental-sea-config sea-config.json && node -e \"fs.copyFileSync(process.execPath, '无忧音乐播放器.exe')\" && npx postject 无忧音乐播放器.exe NODE_SEA_BLOB sea-prep.blob --sentinel-fuse NODE_SEA_FUSE_fce680ab2cc467b6e072b8b5df1996b2 && rm -f _embedded.js bundle.js sea-prep.blob"
  },
  "pkg": {
    "assets": [
      "public/**/*"
    ],
    "targets": [
      "node18-win-x64"
    ]
  },
  "dependencies": {
    "esbuild": "^0.28.0",
    "express": "^4.18.2"
  }
}
